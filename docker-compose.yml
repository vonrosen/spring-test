version: "3.3"
services:
   webapp:
      build: .
      image: spring-test
      network_mode: host
      ports:
         - "8080:8080"
   db:
      image: postgres
      ports:
         - "5432:5432"